<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeamGator"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".Reservation"
            android:label="Reservation"
            ></activity>

        <activity android:name=".Faq"
            android:label="Feedback"
            ></activity>

        <activity android:name=".Survey"
            android:label="Survey"
            ></activity>

        <activity
            android:name=".ReviewActivity"
            android:label="Review Activity" />

        <activity
            android:name=".ReviewActivity2"
            android:label="Review Activity2" />

        <activity
            android:name=".MenuMain"
            android:label="Menu Main">
            <!-- Add any other necessary attributes here -->
        </activity>
        <activity
            android:name=".NormalMenu"
            android:label="Normal Menu">
            <!-- Add any other necessary attributes here -->
        </activity>
        <activity
            android:name=".SpecialMenu"
            android:label="Special Menu">
            <!-- Add any other necessary attributes here -->
        </activity>
        <activity
            android:name=".Game"
            android:label="Game Session">
            <!-- Add any other necessary attributes here -->
        </activity>
    </application>

    <uses-feature android:name="com.softbank.hardware.pepper"/>
</manifest>