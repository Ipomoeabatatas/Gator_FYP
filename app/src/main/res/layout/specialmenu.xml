<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    tools:context=".SpecialMenu">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/imageView3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:ignore="MissingConstraints">

            <ImageView
                android:id="@+id/imageView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scaleType="centerCrop"
                android:src="@drawable/specmenu"
                android:adjustViewBounds="true"
                android:layout_gravity="top" />

            <!-- Add more content here to allow scrolling -->
            <!-- For example, you can add additional buttons, text, images, etc. -->
            <!-- ... -->

            <Button
                android:id="@+id/ExitButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                android:layout_gravity="start|bottom"
                android:textColor="@color/Black"
                android:textSize="15sp"
                android:textStyle="bold|italic"
                android:backgroundTint="@color/Wheat"
                android:text="EXIT" />

            <Button
                android:id="@+id/ExitButtonMain"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                android:layout_gravity="bottom|right"
                android:textColor="@color/Black"
                android:textSize="15sp"
                android:textStyle="bold|italic"
                android:backgroundTint="@color/Wheat"
                android:text="HOMEPAGE" />

        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
